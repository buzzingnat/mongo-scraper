<form id="scrapeSiteForm">
	<label for="scrapeTarget">More cute pictures of:&nbsp;</label>
	<select name="scrapeTarget" id="scrapeTarget">
		{{#each subreddits}}
			<option value="{{@index}}">{{this}}</option>
		{{/each}}
	</select>
	<button type="submit">Submit</button>
</form>
